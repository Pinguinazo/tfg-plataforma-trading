services:
  backend-dev:
    image: node:22-alpine
    container_name: api_backend_tfg
    working_dir: /app
    volumes:
      - ./api:/app
    ports:
      - "3001:3001"
    command: sh -c "npm install && npm run dev"