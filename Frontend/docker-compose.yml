services:
  frontend-dev:
    image: node:22-alpine
    container_name: web_frontend_tfg
    working_dir: /app
    volumes:
      - ./webapp:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev"